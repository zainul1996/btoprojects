# Decision Log (ADR-lite)

Append-only. Significant decisions get an ID, date, context, decision, and consequence. Reversals are new entries referencing the old ID — never edit history.

## Decided

| ID | Date | Decision | Context & consequence |
|---|---|---|---|
| D0 | 2 Aug 2026 | Build a decision platform, not a news site | Per strategy PDF. Consequence: all MVP scope prioritises structured lifecycle data, comparison and alerts over editorial content. |
| D-stack | 2 Aug 2026 | Next.js (App Router) + Vercel + Convex | Per strategy PDF. No AWS at launch; add infra only on measured bottleneck. |
| D-auth | 2 Aug 2026 | Clerk with Google sign-in; no auth wall for browsing | Fast account flow; Convex integration via Clerk JWT. Gate saves/alerts/history only. |
| D-ai | 2 Aug 2026 | LLM behind provider abstraction; DeepSeek as initial candidate | Model landscape is fast-moving; must stay swappable. Benchmark latency/quality/SG-context/data-handling before committing. Retrieval-first: LLM never answers facts from memory. |
| D-data | 2 Aug 2026 | Provenance-first data model | Every field: value, source URL, retrieved-at, effective date, extraction method, confidence. Consequence: schema carries `projectFacts`/`sources`/`reviewQueue` from day one. |
| D-env | 2 Aug 2026 | CLIs via `npx`, pinned as devDependencies | Convex 1.43.0 / Vercel 58.4.4 verified working via npx; neither installed globally. Node 26 works but Vercel CLI warns (engines ≤ 24) — drop to Node 24 LTS if issues appear. |
| D7 | 2 Aug 2026 | GitHub repo: `zainul1996/btoprojects` | Empty repo created by user; gh CLI has push access. Local-only for now — no Vercel deploy until user asks. |
| D6 | 2 Aug 2026 | OpenRouter as LLM gateway; initial model `deepseek/deepseek-v4-flash-0731` | User-provided key. OpenRouter IS the provider abstraction — swap models via config. Log cost/latency per task. |
| D5 | 2 Aug 2026 | Resend excluded from initial build | Per user: alert engine + in-app notification feed + delivery logs. Interim delivery via Telegram bot adapter (user ID 46868450) once real bot token provided; Resend wired later. |
| D-seed | 2 Aug 2026 | MVP data sourcing: curated seed of ~10 real 2026 projects + live data.gov.sg/OneMap adapters; HDB page-fetcher built but snapshot-based | User-approved. Politer on HDB servers, faster, deterministic. Live HDB fetching deferred to pre-launch hardening. |
| D-flow | 2 Aug 2026 | Build runs fully autonomous, single final review | Per user — no mid-run checkpoints. Quality gates (lint/typecheck/review subagents) run between stages instead. |
| D-cred | 2 Aug 2026 | Credentials received: OneMap, data.gov.sg dev key, OpenRouter, Clerk test keys | Stored ONLY in `.env.local` (gitignored). data.gov.sg key is dev tier — upgrade to prod before heavy ingestion. |
| D-tg | 2 Aug 2026 | Telegram alerts verified end-to-end | Bot `@Zainultestbot` (token live), test message delivered to chat 46868450. Clerk `convex` JWT template confirmed created by user. |
| D-be1 | 2 Aug 2026 | No `comparisons` table — compare tray is client-side localStorage for MVP | Keeps comparison anonymous-friendly per D-auth (no auth wall for browsing). Server-side saved comparisons revisited post-MVP if users ask. |
| D-be2 | 2 Aug 2026 | `mynicehome.gov.sg` counts as `hdb` source kind (with hdb.gov.sg) | HDB's official sales portal; seed source-kind rule ("hdb.gov.sg → hdb, else publisher") extended accordingly. |
| D-be3 | 2 Aug 2026 | Schema v1 omits `projectVersions`, `reviewQueue`, `alertDigests` | Field-level `projectFacts` + `ingestionJobs` cover MVP provenance and parser monitoring; versions + review queue land with the HDB parser pipeline (F1.3/F1.8). |
| D-be4 | 2 Aug 2026 | Planner ranking is deterministic code; LLM only extracts constraints + narrates with [slug] citations | Implements D-ai/D-data guardrails. Ranking weights fixed at 35/25/20/20 (budget/wait/flatType/location) with human-readable reasons; narration failure falls back to a deterministic reply. |
| D-ux1 | 2 Aug 2026 | Validation fixes: watchlist town links via shared `townHref()` (Kallang/Whampoa 404 bug); compare table now SSR via `fetchQuery` (shared links first-paint with real content); watchlist gate SSR via server `auth()` (no flash) | All three surfaced by the validation agent's smoke + UX audit; fixed in-code rather than documented away. |
| D-ux2 | 2 Aug 2026 | Accepted MVP deviations from DESIGN.md: (a) planner constraint chips read-only (correction via chat reply — doc amended); (b) mobile explorer stacks map above list instead of full bottom-sheet-over-map (filters do get a sheet) | Deliberate scope calls, not accidents. Bottom-sheet explorer + inline chip editing are the first V1.x UI upgrades. |
| D-ux3 | 2 Aug 2026 | Pre-launch items (not MVP blockers): Telegram demo-chat fallback means watchers without a chatId share the env demo chat — skip delivery when no user chatId before multi-user; Next 16 `middleware`→`proxy` deprecation warning — rename with Clerk docs verified | Logged from validation's "anything else" notes so they aren't lost. |
| D-ux4 | 2 Aug 2026 | Planner chat moved from the Convex action to a Next.js route handler (`/api/planner/chat`) using AI SDK 7 streaming: transient `data-phase` progress parts, `data-rankings`/`data-constraints` message parts, streamed markdown narration, reasoning tokens excluded (OpenRouter `reasoning: {exclude, effort:"low"}`) | User asked for t3.chat-style live status + low TTFT + real markdown. Convex can't stream action responses; the route handler keeps the same guardrails (extraction → deterministic ranking → cited narration) and shares pipeline code via `convex/lib/plannerShared.ts`. Persistence moved client-side via new public `planner.saveTurn` authedMutation + public `planner.forRanking` query (project data is public). The Convex action remains as the non-streaming path. |
| D-ux5 | 2 Aug 2026 | Site-wide copy sweep against 2026 AI-slop tells: zero em dashes in user-facing prose (periods/colons/commas), slop vocabulary verified absent | Grounded in current guidance (voicemoat/telltale checklists). Em dashes kept only where typographically correct: title separators, range en dashes, muted-cell placeholders. |
| D-ing1 | 3 Aug 2026 | Ingestion pipeline v2: per-source modules under `convex/ingest/` sharing a guarded framework lib; official-first source policy (robots checked, datasets over scraping, tiny request volumes) | Multi-agent build (3 parallel crawlers + verifier). Framework guarantees: every fact carries source/confidence/method, "official" facts can never be downgraded, every run leaves an ingestionJobs row. Legacy `datagov.syncResale` cron superseded. |
| D-ing2 | 3 Aug 2026 | Resale ingestion semantics: incremental month-replace (newest stored month always re-ingested for late registrations) + kv cursor; all SG towns, not just seed towns | data.gov.sg resale CSV is append-only with late updates; month-replace keeps us exact without row-level dedup cost. First run bounded to newest 13 months (~27k rows). |
| D-ing3 | 3 Aug 2026 | HDB launch data from Flat Portal application-rate JSON, not page scraping | `services-homes.hdb.gov.sg` robots "Allow: /", static JSON per exercise. Yields windows/classification/flat supply but no prices/completion/coords: those stay seeded until a MyNiceHome parser track. New-project shells get placeholder zeros + town-fallback geocoding; (0,0) markers never render. |
| D-ing4 | 3 Aug 2026 | Announced-but-not-launched exercises are seeded from verified press reporting with honesty labels; launch-day reconciliation is automatic for 1:1 towns, human for multi-shell towns | October 2026 (7,970 flats, 7 projects, HDB-announced 17 Jun 2026) seeded as `lifecycleStatus: "announced"`: official unit counts/flat mixes, working-title names, analyst-expected classifications, MRT-anchored coords (both "estimated"). Zero flatTypes rows until prices exist; excluded from planner rankings; area markers on the map. `adoptAnnouncedShell` upgrades announced rows in place when real launch data arrives (Bayshore I/II ambiguous → job conflict note for human merge). Announcement-gap policy: MyNiceHome-first weekly crawler planned (official portal, no WAF) — headless-browser bypass of www.hdb.gov.sg unnecessary. |
| D-ux6 | 3 Aug 2026 | Planner moves to a tool-calling agent loop (supersedes D-ux4's extract-then-rank pipeline); `useChat` lifted into a layout-level `PlannerChatProvider` | The old pipeline answered only ranking questions and was blind to announced projects (forRanking excludes them by design). Now one LLM loop picks read-only tools: searchProjects / getProjectDetail / rankProjects (deterministic scorer still owns recommendations) / getResaleMedian (new `convex/resale.ts` townMedian) / listExercises / getTownOverview, plus Tavily `webSearch` for anything outside our records (amenities, landmarks) with explicit "from the web" labeling. Extraction-by-tool-call replaces the separate extraction LLM hop (schema-safe, one less failure mode); constraint memory rides `priorConstraints` into the tool args. Post-stream verification now checks citations/amounts against the union of ALL tool results this turn (±2% rounding tolerance). Provider lift: navigating away mid-reply no longer loses the answer; sessionStorage still covers reloads. |
| D-ux7 | 3 Aug 2026 | Planner is a whole-page chat: route-aware `AppShell` drops the footer and viewport-locks `/planner`; chat scroll region is full-bleed with a centered content column | Wheel-over-gutter used to scroll the document to the footer. Now the document cannot scroll on /planner and the full-width scroll container catches gutter wheel events (ChatGPT/Claude-style). Other routes unchanged. |
| D-ux8 | 3 Aug 2026 | Keyboard-height override is focus-driven, with an explicit repair for the iOS 26.0 stuck-viewport-pan WebKit bug | iOS 26.0 leaves `visualViewport.offsetTop > 0` after keyboard dismissal (fixed only in 26.1), which read as "keyboard still open" and pinned the composer mid-screen. The hook now keys off `document.activeElement` (null override on blur), nudges `window.scrollTo(0,0)` when the keyboard is closed but the pan persists, and re-checks after the 350ms dismiss animation. Composer bottom padding drops to 0.5rem while the keyboard is open (safe-area inset is under the keyboard). Sources: Apple Developer Forums thread 800125, WebKit bug 301857. |
| D-sbf1 | 3 Aug 2026 | SBF is modeled as `projects` rows (one per SBF exercise × town) with denormalized `saleType: "bto" \| "sbf"`; classification union widened with `"Unclassified"` | A separate sbfOfferings table would have forked every query, card, map layer, compare row, alert and planner tool. Putting SBF on the projects spine is what "woven into every part" means mechanically. SBF pools mix classifications (HDB publishes "NA"), so honesty required a fourth classification literal rather than overloading Standard. lifecycleStatus stays orthogonal (announced → launched); the legacy "sbf" lifecycle literal is unused by this model. |
| D-sbf2 | 3 Aug 2026 | SBF town pools use town-centroid area markers, zero wait/completion placeholders, and are excluded from `forRanking`/`allForRanking` | Pools span the town (no site pin); many flats are completed so "wait" is short but per-flat — 0 with honest copy, never a fabricated number. Without per-unit prices the deterministic scorer would mislead, so the planner narrates SBF via searchProjects/getTownOverview and keeps rankProjects BTO-only. |
| D-sbf3 | 3 Aug 2026 | SBF pool display names are "{Town} balance flats"; type and exercise context live in badges/chips, never in the name | Launch naming ("{Town} balance flats (February 2026 SBF)") stuffed metadata into the identity field; user review caught it. Slugs (`sbf-2026-02-{town}`) carry uniqueness and stay stable; rename was display-only (`seedSbf:renamePools`). |
| D-sbf4 | 3 Aug 2026 | "Expected next SBF" is a cadence prediction (every February alongside the Feb BTO since 2024 → next: Feb 2027), seeded as an upcoming exercise row `2027-02-sbf`; composition is never predicted as fact | SBF composition is revealed only on launch day. The honest prediction is the window plus last-pool stats (Feb 2026: 4,320 units / 24 towns / ~1 in 5 completed per HDB), labeled "expected, not official" like the BTO analysis card. |
| D-sbf5 | 3 Aug 2026 | Open Booking of Flats and unit-level SBF listings are deliberately out of scope (P3 revisit) | Open booking is rolling first-come-first-served, a different sales model with no ballot; unit-level listings live behind the Flat Portal SPA (API unexplored). Both noted in DATA_SOURCES.md; press-release starting-price extraction (secondary mirrors) is the P3 price track. |
| D-seo1 | 3 Aug 2026 | Use btoprojects.sg as the only canonical origin, keep preview deployments out of robots, and describe public routes with visible source copy plus matching structured data | Search and answer engines receive one stable URL per page. Public content uses WebSite, FAQPage, AboutPage, CollectionPage, ItemPage and BreadcrumbList schema. Project social cards are generated from the same Convex record as the page. data.gov.sg reuse carries a linked Singapore Open Data Licence notice. |
| D-seo2 | 3 Aug 2026 | Use https://btoprojects.vercel.app as the canonical origin until a custom domain is purchased and migrated | Supersedes D-seo1's origin only. The product name remains BTOProjects.sg. A future domain change requires one canonical update plus permanent redirects from the Vercel origin. |

## Open questions (resolve before scaffolding — F0.3)

| ID | Question | Options | Lean |
|---|---|---|---|
| D1 | Package manager | npm (present, v11) · pnpm 10 (Vercel ecosystem preference) | npm — simplest, zero install; revisit if workspace/monorepo needs emerge |
| D2 | Monorepo or single app | Single Next.js app with `convex/` inside · pnpm workspaces | Single app — MVP simplicity; ingestion lives in Convex actions |
| D3 | Basemap strategy | OneMap default styles · self-hosted/custom vector tiles (e.g. Protomaps) · commercial (MapTiler/Mapbox) | Evaluate OneMap styles first; strategy forbids assuming OneMap tiles are restylable. Custom light basemap is a design requirement. |
| D4 | Component/UI foundation | Tailwind + shadcn/ui · Tailwind + Radix only | Tailwind + shadcn/ui — speed + the calm, spacious look is achievable; tokens per PRODUCT.md |
| D5 | Email provider | Resend · Postmark | Resend — React Email fits the stack; Postmark if deliverability issues |
| D6 | LLM provider wiring | Direct SDK per provider · Vercel AI SDK as the abstraction | Vercel AI SDK — native Next.js streaming, provider-swappable |
| D7 | Git hosting / CI | GitHub + Vercel Git integration | GitHub — default; confirm account/repo name with user |

## Deferred decisions (not needed yet)

| ID | Question | When to revisit |
|---|---|---|
| D-later-1 | PostHog vs Vercel Analytics only | When funnels/experiments justify (post-launch) |
| D-later-2 | Cloudflare R2 vs Vercel Blob for snapshots | When snapshot volume/cost is measurable |
| D-later-3 | Push notifications (web push vs app) | V2, with draw-an-area alerts |
| D-later-4 | Mobile app (PWA vs native) | "Later" phase per roadmap |
